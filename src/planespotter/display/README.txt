package: planespotter.display

- GUI opens automatically when the program starts
- represents a GUI with menu, title and different views
- GUI class is the main GUI, other classes just contain GUI-models or GUI-functions